


Livewire.on('showModal', (id) => cash(`#${id}`).modal('show'));

// cash(document).on('livewire:load', () => {

    // Livewire.ho
    //     initialiok('component.initialized', (message, component) => {zeTomSelects();
    // });

    // window.addEventListener('htmlPrint', event => {
    //     var w = window.open("about:blank");
    //     w.document.write(event.detail.html);
    //     w.print();
    //     w.close();
    // });

    // $
    // window.addEventListener('renderTom', event => {
    //     initializeTomSelect();
    //     console.log('se ejecuto');
    // });



//     Livewire.hook('message.processed', (message, component) => {

//         console.log('se ejecuto');

//         // initializeDatepicker();

//         // initializeTomSelects();

//         // initializeTailSelect();
//         // initializeTomSelect();

//         // initializeTooltips();

//         window.initializeFeather();

//         // initializeSvgLoader();

//         // initializeMaps();

//         // window.initializeCharts();

//         Object.entries(datepickers).forEach(([key, value]) => {
//             cash(`input#${key}`).val(value);
//         });

//     });

//     Livewire.on('showModal', (id) => cash(`#${id}`).modal('show'));

// });
